name: rongwang-prod

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL}
    env_file:
      - .env.production
    network_mode: host
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 127.0.0.1
      DATABASE_URL: postgresql://rongwang:rongwang_prod_2024@localhost:5432/rongwang?schema=public
      DIRECT_URL: postgresql://rongwang:rongwang_prod_2024@localhost:5432/rongwang?schema=public
      REDIS_REST_URL: http://localhost:8079
      REDIS_REST_TOKEN: ${REDIS_REST_TOKEN}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      redis-rest:
        condition: service_started
    restart: unless-stopped
    volumes:
      - /root/rongwang-health-platform/public/_geoflow_cache:/app/public/_geoflow_cache:ro
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "node -e \"fetch('http://127.0.0.1:3000/api/health').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\"",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-rongwang}
      POSTGRES_USER: ${POSTGRES_USER:-rongwang}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-rongwang} -d ${POSTGRES_DB:-rongwang}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis-rest:
    image: hiett/serverless-redis-http:latest
    environment:
      SRH_MODE: env
      SRH_TOKEN: ${REDIS_REST_TOKEN}
      SRH_CONNECTION_STRING: redis://redis:6379
    depends_on:
      redis:
        condition: service_healthy
    ports:
      - "127.0.0.1:8079:80"
    restart: unless-stopped

volumes:
  postgres-data:
  redis-data:
